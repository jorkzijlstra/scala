package objsets

import objsets.Empty

object Test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(72); 
  val x = new Empty;System.out.println("""x  : objsets.Empty = """ + $show(x ))}
  
  
  
}
