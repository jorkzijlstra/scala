package objsets

object Blaat {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(51); 
  val x = new Empty;System.out.println("""x  : objsets.Empty = """ + $show(x ))}
  
}
